name: CodeQL config — suppress test false positives

# Exclude test directories from analysis entirely
paths-ignore:
  - crates/kesh-api/tests/
  - crates/kesh-db/tests/
  - frontend/src/
  - frontend/

# Disable overly-noisy security checks that produce false positives in test code
disable-default-queries: false
