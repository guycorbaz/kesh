# Rétrospective — Epic 14 « Clôture d'exercice & report à-nouveau »

**Date** : 2026-07-25
**Statut** : ✅ **CLÔTURÉE** — les 4 décisions Project Lead ont été tranchées le **2026-07-26** (cf. §7).
Reste hors rétrospective : ouvrir la PR groupée Epic 14 (A1), poser 2 labels GitHub (A3/A7 — bloqués sur `gh auth login`), puis la release v0.8.0 (A9, gate humain).
**Statut epic** : 6/6 stories done (14-1, 14-2, 14-3a, 14-3b, 14-3c, 14-4). Branche `story/14-4-bilan-ouverture-soldes-depart` = sommet du stack, poussée, **PR non ouverte**.
**Rétro précédente** : `epic-21-retro-2026-07-20.md` (Epic 21, dernier epic chronologique — l'Epic 14 est numéroté 14 mais exécuté après 21, cf. ordre v0.2).

---

## 1. Résumé de livraison

L'Epic 14 livre le **bouclement d'exercice complet** pour une PME suisse, sur un modèle de consolidation **temps réel virtuel** (Odoo/Flectra) : aucune écriture de clôture ni de report n'est générée, tout est calculé.

| Story | Livré | Passes de revue (validate + code-review) |
|---|---|---|
| **14-1** | Bilan cumulatif cross-exercice, report à-nouveau et résultat de l'exercice calculés, retrait du hardcode `EQUITY_RESULT_ACCOUNT_NUMBERS` | 3 + 2 = **5** |
| **14-2** | Réouverture d'un exercice clôturé (Admin, motif obligatoire borné, audit `fiscal_year.reopened`, garde LIFO stricte, anti-PAT) | 5 + 3 = **8** |
| **14-3a** | Socle des rôles de comptes (10 rôles, unicité structurelle des 8 singletons via colonne générée, `postable`, réactivation d'un compte archivé #269) | 3 + 3 = **6** |
| **14-3b** | Consommateurs des rôles (garde de postabilité à la saisie manuelle, 6 derniers lookups par numéro éliminés) | 2 + 3 = **5** |
| **14-3c** | Fonds propres présentés par rôle au bilan (section dédiée CO 959a, distinction compte physique / ligne calculée) | 3 + 3 = **6** |
| **14-4** | Bilan d'ouverture — écran « Soldes de départ » (reprise de comptabilité, écriture OD équilibrée datée au 1er jour du 1er exercice) | 5 + 5 = **10** |

**Métriques** : 56 commits · 105 fichiers · +14 818 / −352 lignes · **1 seule migration** (`20260722000001_accounts_role_postable.sql`) · **40 passes adversariales** au total.

**Qualité** : gate final **2018 tests backend** (0 échec, 4 skipped pré-existants) contre 1898 au démarrage de l'epic → **+120 tests**. Frontend 456 vitest + check 0 erreur + lint-i18n PASS + build. Playwright 6/6 réels. Aucune régression sur toute la durée de l'epic.

**Empreinte modules** : frontend (33 fichiers), `kesh-api` (21), `kesh-db` (20), `kesh-report` (5), `kesh-i18n` (4), `kesh-core` (4), docs/manuels (6).

---

## 2. Suivi de la rétro Epic 21 — 6 actions sur 7 ✅

| Action | Statut | Preuve |
|---|---|---|
| A1 — Ouvrir la PR Epic 21 | ✅ | mergée `1c4d42e5` |
| A2 — Créer l'issue export souveraineté | ✅ | #262 créée **et fermée** |
| A3 — Fix #259 (rappel manuel time-of-day) | ✅ | fermée (mini-lot `eb744a82`) |
| A4 — Fix #260 (garde suppression ↔ rappels) | ✅ | fermée (mini-lot) |
| A5 — Trancher cat A/B de #256 (a11y) | ✅ | tranché cat A par Guy, **fixée et fermée** avant v0.7.0 |
| A6 — **Clore le reliquat 12-5** | ❌ | **12-5 toujours en `review`**, `epic-12` toujours `in-progress` |
| A7 — Planifier #252 + séquencer #258 | ✅ | tranché (mini-lot dette → release v0.7.0) et **respecté** |

**Taux de suivi : 6/7.** Le séquencement décidé en A7 a été exécuté à la lettre : mini-lot dette (#256/#259/#260/#262), puis release v0.7.0, puis feature.

⚠️ **A6 est un carry-forward signalé pour la deuxième rétro consécutive** — exactement ce que la politique zero-carry-forward interdit. Décision demandée en §7.

---

## 3. Ce qui a bien marché

1. **Un principe architectural tenu sur tout l'epic.** Guy pose en 14-1 : *« chaque compte a un rôle explicite, jamais déduit de son numéro »* (le plan comptable suisse est un usage, pas une obligation légale). **Quatre stories sur six** l'exécutent bout en bout : 14-1 retire le hardcode `EQUITY_RESULT_ACCOUNT_NUMBERS`, 14-3a crée les rôles, 14-3b élimine les 6 derniers lookups par numéro, 14-3c partitionne le bilan par rôle. L'epic se termine **sans un seul numéro de compte codé en dur dans la logique métier**.

2. **Le split préventif a payé — et le re-split proactif encore plus.** 14-3 → 14-3a/14-3b, puis 14-3b → 14-3b/14-3c. Aucune sous-story n'a dépassé **3 passes** pour converger sur une phase (seuil de la règle : 4). Surtout, le re-split a été décidé **avant le dev**, sur un jugement qualitatif (« le chantier B est de la conception, pas du rollout mécanique »), pas en réaction à une non-convergence observée. 14-3c a produit à elle seule 1 CRITICAL + 4 HIGH en validate P1 : la garder dans 14-3b aurait fait dérailler cette dernière.

3. **Fix structurels systématiques sur les bugs de classe.** Trois exemples documentés comme tels :
   - 14-1 : l'équation du bilan rendue **vraie par construction** (garde `DateOutsideFiscalYear` ajoutée à `create_in_tx`, symétrique de `update`) plutôt que reposant sur le filet `equation_holds`.
   - 14-3c : clé `{#each}` = identité intrinsèque (`group.role`) plutôt qu'index positionnel.
   - 14-4 : sentinel `companies FOR UPDATE` — la garde étant company-wide, le verrou qui la protège l'est aussi.

4. **Cartographie ground-truth par agents Explore avant chaque spec** (2 à 3 agents par story). Les reviewers ont validé les ancres `fichier:ligne` comme exactes à chaque passe (~40 ancres vérifiées par story). Zéro faux-ancrage constaté en dev.

5. **La discipline grep ground-truth a réfuté toutes les hallucinations.** 2 hallucinations Haiku réfutées en 14-4 (colonnes `DECIMAL(19,4)` vs « 19,2 » affirmé ; `Language` non-`Option`), 2 faux positifs Sonnet réfutés en 14-1. Le garde-fou CLAUDE.md fonctionne — et il vaut **pour tous les modèles**, pas seulement Haiku.

---

## 4. Difficultés & leçons

### 4.1 « La remédiation crée le défaut suivant » — attesté **5 fois** dans le seul Epic 14

| Où | Ce qui s'est passé |
|---|---|
| 14-3a validate P1→P2 | Le patch du CRITICAL pose `is_singleton()` sur un seul des deux enums → HIGH en P2 (Opus) |
| 14-3c review P1→P2 | Sonnet remplace une clé `{#each}` fragile par un index positionnel ; **Haiku démontre que le patch est pire** que l'original (décalage DOM si un groupe disparaît) |
| 14-2 validate P3→P4 | Le patch corrige le fallback svelte `:405`, oublie `:428` → 2 MED en P4 |
| 14-4 validate P3→P4 | Le fix `count_by_company` laisse 3 reformulations contradictoires → 1 MED en P4 |
| 14-4 review P3→P4 | L'amendement de priorité n'est propagé qu'au `GET /status`, pas au `POST` ni au repo → 1 MED convergé BH+ECH en P4 |

**Ce n'est plus une leçon ponctuelle, c'est une propriété structurelle du processus** : elle est signalée à chaque rétrospective depuis l'Epic 21 et continue de se produire. Elle est déjà codifiée (CLAUDE.md « un patch de review vient AVEC son test », memory `feedback_review_patch_needs_test`) — la codification **n'a pas suffi**.

**Piste concrète (à valider)** : ajouter à la boucle de remédiation une étape explicite de **propagation** — après tout patch, `grep` la formulation/le symptôme corrigé sur *tout* le dépôt (code + spec + doc-comments + tests + i18n) et lister les sites atteints avant de relancer la passe suivante. Les 5 occurrences ci-dessus auraient toutes été attrapées par ce seul geste.

### 4.2 Une spec scellée n'est pas une preuve

Le défaut le plus grave de l'epic — **doublon d'écriture d'ouverture** en 14-4 (deux requêtes concurrentes résolvant des « premiers exercices » différents verrouillent des lignes distinctes, les deux passent, **bilan doublé en silence**) — a été trouvé par **Opus en code-review passe 3**, c'est-à-dire :

- après **15 revues de spec** (5 passes × 3 agents) qui ont scellé le document `ready-for-dev` ;
- après **2 passes de code-review** (Sonnet, Haiku) ;
- alors que la spec **décrivait une variante** du problème dans sa limitation L4 et concluait explicitement *« mauvais millésime, pas de doublon »* — conclusion fausse.

**Leçon** : la spec sécurise la *conception*, pas le *modèle de menace*. Une code-review ne doit jamais présumer que le threat model de la spec est complet, en particulier sur les **compositions à plusieurs requêtes** (le cas ici : requête A résout FY2026, un tiers crée FY2025, requête B résout FY2025).

### 4.3 Orthogonalité des modèles : ce qui est vraiment mesuré

Opus a trouvé le défaut décisif dans **4 stories sur 6**, systématiquement après que Sonnet et/ou Haiku soient passés à côté (14-1 P2 sémantique `period_start` ; 14-2 P3 dialogue de clôture mensonger, manqué par 2 passes complètes ; 14-4 validate P3 double-ouverture ; 14-4 review P3 doublon multi-FY).

**Mais** c'est **Haiku** qui a démoli le patch de Sonnet en 14-3c. Conclusion nuancée : ce n'est pas « Opus est meilleur », c'est **l'orthogonalité qui paie** — avec un avantage net et reproductible d'Opus sur les défauts **compositionnels et architecturaux**, là où Sonnet/Haiku excellent sur le ground-truth ligne à ligne.

**Piste** : envisager de placer Opus **plus tôt** dans le cycle sur les stories à forte densité de concurrence/état (transactions, verrous, courses), plutôt qu'en 3ᵉ position systématique.

### 4.4 Le signal de la règle de splitting a sonné deux fois sans être suivi

La règle CLAUDE.md déclenche un split si *« `validate` boucle au-delà de 4 passes sans converger »*. Le signal a sonné sur **14-2** (5 passes validate) et **14-4** (5 passes validate + 5 de code-review, la story la plus chère de l'epic). Aucune des deux n'a été splittée.

**Débat interne non tranché** : les deux ont convergé avec une sévérité **strictement décroissante** (14-4 : 1 CRIT/2 HIGH/6 MED → 0 → 3 MED → 1 MED → 0). Ce n'est pas « ne converge pas », c'est « converge lentement ». La règle, formulée en nombre de passes, ne distingue pas les deux cas. Décision demandée en §7.

### 4.5 Gaps de traçabilité mineurs

- La section « Dev Agent Record » de **14-3b** est restée avec ses placeholders vides, contrairement à 14-3a/14-3c. L'information existe dans le Change Log de code-review mais n'est pas structurée.
- **L3 de 14-3c** (un rôle equity posé sur un compte de type `Asset` resterait hors de la section fonds propres — cas rare, arithmétiquement sain) est documentée comme limitation catégorie B **sans issue GitHub ni label `v0.2-milestone`** : elle n'a donc pas de mécanisme de planification, ce que la politique de triage exige pour qualifier en B.

### 4.6 Dérive des statuts sprint

- **`epic-21` est resté `in-progress`** alors qu'il est mergé sur `main` (`1c4d42e5`), sa rétro faite et la release v0.7.0 publiée.
- **`epic-12` est resté `in-progress`** avec 12-5 en `review` (cf. A6, §2).

Cause : on flippe méthodiquement les clés de *story* à chaque étape, mais la clé d'*epic* n'est mise à jour qu'au moment de la rétro — et si la rétro d'un epic ne re-vérifie pas les epics *antérieurs*, la dérive persiste.

---

## 5. Triage des dettes (politique zero-carry-forward)

### Catégorie A — vraie dette (à fixer OU reclasser B avant le kickoff suivant)

| Item | Origine | Recommandation |
|---|---|---|
| **Reliquat 12-5** en `review` + `epic-12` `in-progress` | Epic 12 (juin) | ✅ **RÉSOLU** (D-B) — bookkeeping seul, PR #200 mergée : `12-5` + `epic-12` → `done` |
| **Dérive statuts** `epic-21` / `epic-12` dans sprint-status | hygiène process | ✅ **RÉSOLU** — `epic-21` → `done` (mergé + rétro faite) |
| **#274** — retyper un compte à historique reclassifie silencieusement bilan/P&L | découverte code-review 14-4 (cause racine pré-existante) | ✅ **reclassé cat B** (D-A) — label `v0.2-milestone` à poser (A3) |
| **L3 de 14-3c** sans mécanisme de planification | 14-3c | ✅ **RÉSOLU** — issue **#276** créée + label `v0.2-milestone` ⇒ cat B en règle |

### Catégorie B — limitation v0.2 légitime (tracée, remédiation planifiée)

| Item | Trace |
|---|---|
| **#270** — snapshot des soldes de clôture (perf) | issue + `v0.2-milestone` ; invalidation à câbler sur `fiscal_year.reopened` (L3 de 14-2) |
| **#271** — `<select>` config affiche vide une valeur non-postable persistée | issue + `v0.2-milestone` (L5 de 14-3b) |
| **#269** — réactivation d'un compte archivé | ✅ **livrée par 14-3a** (issue à fermer au merge) |
| #252 (deps), #251 (perf tests), #253, #255, #257 | hérités Epic 21, tracés |
| #258 — RBAC configurable | chantier plateforme, décision de séquencement §7 |

### Catégorie C — décision design intentionnelle (pas une dette)

- **14-1** : modèle virtuel pur (aucune écriture physique de clôture/report) ; ancrage du split résultat à `fy_start`.
- **14-2** : L1 motif non requêtable (audit JSON assumé) ; L2 code 409 partagé avec messages distincts ; L4 `close` sans garde LIFO symétrique (pré-existant, sans corruption possible en modèle virtuel).
- **14-3b** : L2 flux automatiques exemptés de la garde de postabilité (délibéré — sinon le moteur de facturation casse) ; L3 grandfather par compte et non par ligne.
- **14-3c** : L1/L2 export FR-CH et distinction textuelle physique/calculé.
- **14-4** : L1 date du 1er exercice non choisie ; L2 pas d'auto-équilibrage ; L3 pas de ré-édition assistée ; L4 garde « company vierge » company-wide ; L5 code d'erreur distinct sous verrou.

### Volume cat-A et Epic dédié ?

**4 items cat-A**, tous ponctuels et localisés (1 reliquat, 1 hygiène de statut, 1 KF pré-existante, 1 traçabilité). **Très en dessous du seuil** (> 8 items OU > 2 axes) d'un Epic « Technical Debt Closure » dédié. Recommandation : **traiter dans un mini-lot** ou en préalable du prochain epic, comme cela a fonctionné avant la v0.7.0.

---

## 6. Prochaine étape & release

- **PR Epic 14** : à ouvrir (branche poussée, 56 commits > `main`). Fermerait #269 (réactivation compte archivé, livrée par 14-3a).
- **Release v0.8.0** : gate humain Guy. Au tag, appliquer le **gate 4-bis** — bump `\keshVersion{0.8.0}` / `\keshReleaseDate` / `\keshTargetRelease{v0.8}` dans `docs/manual/shared/kesh-style.sty` + **régénérer les 3 PDF** (`make fr`) + doc-sync website/roadmap + CHANGELOG daté. Le **contenu** des manuels est déjà à jour (14-2 et 14-4 ont ajouté leurs sections) ; seules les macros de version restent à bumper.
- **Pas de bump `min_required`** : l'unique migration de l'epic est un `ADD COLUMN` + index, donc non-breaking (politique P1/P2).

---

## 7. ✅ Décisions Project Lead (tranchées le 2026-07-26)

> Les 4 décisions posées le 2026-07-25 ont été tranchées par Guy le 2026-07-26. Elles débloquent la clôture de la rétrospective.

**D-A — Triage de l'issue #274** (retyper un compte qui porte des écritures reclassifie silencieusement son historique au bilan et au compte de résultat) → **Catégorie B**.

Pré-existante depuis l'Epic 3, jamais rencontrée en usage réel, exige une manipulation délibérée d'un Comptable+. Traçage : label `v0.2-milestone` sur #274, remédiation planifiée au plus tard au kickoff de l'epic qui consommera le backlog v0.2. **Ne bloque pas** le kickoff de l'Epic 16.

**D-B — Reliquat 12-5** → **clos maintenant**.

Vérification faite : ce n'était **pas** un reliquat de travail mais un **défaut de bookkeeping**. La PR #200 (« Épopées 12-4 + 12-5 ») est mergée sur `main` (`9d128249`), les 4 sous-stories 12-5a/b/c/d sont `done` et leurs code-reviews convergées ; seule l'umbrella `12-5-import-repertoire-factures` est restée en `review`, ce qui a maintenu `epic-12` en `in-progress`. Aucun code à écrire. `12-5` et `epic-12` passent à `done` (la rétro Epic 12 est `optional`). **Le carry-forward est coupé** — et la leçon est que le suivi d'action rétro doit vérifier *l'état réel* avant de reporter un item, sinon un simple statut non-flippé se déguise en dette pendant deux epics.

**D-C — Règle de splitting** → **amendée** (CLAUDE.md §"Règle de splitting préventif").

Le second critère passe d'un **compteur de passes** (« au-delà de 4 passes de validate ») à un critère de **sévérité** : splitter si une passe `N+1` remonte une sévérité **≥** à la passe `N` (non-convergence réelle / stagnation). Une convergence lente mais monotone (`CRIT → MED → MED → 0`) reste saine — c'est précisément le profil de 14-2 et 14-4, qui ont dépassé l'ancien seuil et convergé proprement, et c'est en passe tardive (P3 Opus) qu'a été trouvé le défaut le plus grave de l'epic. Le plafond de 8 passes de la §"Review Iteration Rule" reste le garde-fou de budget.

**D-D — Prochain epic** → **Epic 16 « Facturation avancée »**.

#144 (compte produit au catalogue) + #152 (compte produit par ligne). Rationale : l'epic est déjà entamé hors-bande (#151 récap TVA sur facture est mergé via PR #267), donc on termine un chantier à moitié ouvert plutôt que d'en ouvrir un troisième ; et il capte les CR utilisateurs récents (#265 compte revenu/ligne + projet/ligne fournisseur, #266 entête facture/logo, #268 note perso facture). Epic 15 (justificatifs & lettrage), Epic 13 (budgets) et #258 (RBAC configurable) restent en file.

---

## 8. Action items (figés après §7)

| # | Action | Propriétaire | Critère | État |
|---|---|---|---|---|
| A1 | Ouvrir la PR Epic 14 (groupée, 6 stories ; ferme #269) | orchestrateur | PR créée, CI verte | ✅ **PR #275** (58 commits, 107 fichiers, +15 055/−358). `refs #232` et non `closes` : #232 demande aussi l'assistant de bouclement, l'aide aux transitoires et l'intégration des amortissements #222, non livrés |
| A2 | Flipper `epic-21` → `done` dans sprint-status (mergé + rétro faite) | orchestrateur | statut corrigé | ✅ |
| A3 | D-A #274 → cat B : poser le label `v0.2-milestone` | orchestrateur | label posé | ⏳ bloqué — `gh` HTTP 401 |
| A4 | D-B → `12-5` et `epic-12` à `done` (bookkeeping, cf. §7) | orchestrateur | statuts corrigés | ✅ |
| A5 | D-C → amender la règle de splitting dans CLAUDE.md | orchestrateur | § amendé | ✅ |
| A6 | D-D → kickoff Epic 16 (première story : #144 ou #152) | orchestrateur | story créée | ⏳ après merge PR Epic 14 |
| A7 | Créer l'issue + label `v0.2-milestone` pour L3 de 14-3c | orchestrateur | tracé | ✅ **issue #276** (`enhancement` + `technical-debt` + `v0.2-milestone`) |
| A8 | Codifier l'étape de **propagation post-patch** (grep du symptôme sur tout le dépôt avant la passe suivante) dans CLAUDE.md | orchestrateur | § ajouté | ✅ |
| A9 | Release v0.8.0 — gate 4-bis (macros + 3 PDF) + doc-sync + CHANGELOG | **Project Lead (Guy)** | tag poussé | ⏳ gate humain |

---

## 9. Readiness

- **Tests** : gate workspace **2018/0** ; frontend 456 vitest + check/lint-i18n/build ; Playwright 6/6 réels. ✅
- **Doc** : CHANGELOG `[Non publié]` à jour, manuel utilisateur enrichi (réouverture + reprise de comptabilité), README roadmap à jour. PDF **non régénérés** — normal, c'est le gate release 4-bis. ✅
- **Déploiement** : Epic 14 **non mergé, non publié**. Branche poussée, PR à ouvrir. ⏳
- **Migration** : 1 seule, non-breaking (`ADD COLUMN` + index) → pas de bump `min_required`. ✅
- **Blocages** : aucun bloquant technique. Les 4 items cat-A sont soldés (2 résolus, 1 reclassé B, 1 à tracer) ; les 4 décisions Project Lead sont tranchées.
- **Dette cat-A résiduelle bloquant le kickoff Epic 16** : **aucune**. Reste 2 gestes GitHub (A3, A7) dépendants de `gh auth login`.

**Epic 14 : complet, sain, rétrospective clôturée. Kickoff Epic 16 « Facturation avancée » débloqué après merge de la PR groupée.**
